
<title>Connections
</title>
<meta name="viewport" content="initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
 <!-- Link to same CSS for the nav bar and footer, save time/space on CSS for main content-->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"> 
<link href="../assets/styles.css" rel="stylesheet" type="text/css"/>
<header id = "header">





    <div class="container">
    <div class ="header" >
        
            <nav>
                
                <%- include('partials/Header.ejs') %>
                
            </nav>
          
                </div>
            </div>
        </header>
        <main>
            <section class="box">
            <div class = "container">
                <div>
                    <ul>
                        <% for(let i = 0; i < list.length; i++) {
                            if(list[i].topic.toLowerCase() === "offline events") {
                            var link = 'connection/' + list[i].ID;%>
                        <li><a href=<%= link %>><%= list[i].details %></a></li>
                        <% }} %>
                    </ul>
                </div>
                <div>
                    <ul>
                        <% for(let i = 0; i < list.length; i++) {
                            if(list[i].topic.toLowerCase() === "online events") {
                            var l = 'connection/' + list[i].ID; %>
                        <li><a href=<%= l %>><%= list[i].details %></a></li>
                        <% }} %>
                    </ul>
                </div>






                <footer>
                    <div class ="footer">
                        <!--Footer with Copy right and Instagram-->
                        <!-- footer left to use CSS to float left-->
                         
                        <%- include('partials/footer')  -%>
                   </div>
                   </footer>
                 
                </html>